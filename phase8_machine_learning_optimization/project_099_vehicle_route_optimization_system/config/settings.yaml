# config/settings.yaml — Vehicle Route Optimization System
project:
  number: 99
  name: "Vehicle Route Optimization System"

# TODO: add tunable parameters (paths, thresholds, model params, etc.)
paths:
  raw_data: "data/raw"
  processed_data: "data/processed"
  outputs: "outputs"
